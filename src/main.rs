//! pay4what — See what each feature cost you in Claude Code.
//!
//! Token spend per activity, not per session.
pub mod discover;
pub mod parse;

fn main() {
    println!("pay4what — token spend per activity (v0.1.0-dev)");
}
